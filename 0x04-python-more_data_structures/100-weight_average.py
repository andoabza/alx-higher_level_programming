#!/usr/bin/python3
def weight_average(my_list=[]):
    if my_list == 0:
        return 0
    else:
        for g in range(len(rate)):
        rate[g] = rate[g] * (amount[g] / sum(amount))
        rate = sum(rate)
